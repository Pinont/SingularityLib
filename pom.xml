<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.pinont</groupId>
    <artifactId>singularitylib</artifactId>
    <version>2.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SingularityLib</name>
    <description>A Library for Minecraft plugin to help developing Minecraft plugin much easier</description>
    <url>https://github.com/Pinont/SingularityLib</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>Pinont</name>
        <url>https://github.com/Pinont</url>
    </organization>

    <developers>
        <developer>
            <id>pinont</id>
            <name>Pinont</name>
            <url>https://github.com/Pinont</url>
            <roles>
                <role>Owner</role>
                <role>Developer</role>
            </roles>
        </developer>
    </developers>

    <profiles>
        <profile>
            <id>with-shade</id>
            <!-- Optional: still allow activation by property 'shade' if you prefer -Dshade=true -->
            <activation>
                <property>
                    <name>shade</name>
                </property>
            </activation>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>3.5.3</version>
                        <configuration>
                            <!-- Attach shaded artifact instead of replacing the main jar -->
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>shaded</shadedClassifierName>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <filters>
                                <filter>
                                    <artifact>org.reflections:reflections</artifact>
                                    <includes>
                                        <include>org/reflections/**</include>
                                    </includes>
                                </filter>
                            </filters>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <scm>
        <url>https://github.com/Pinont/SingularityLib</url>
        <connection>scm:git:https://github.com/Pinont/SingularityLib.git</connection>
        <developerConnection>scm:git:git@github.com:Pinont/SingularityLib.git</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/Pinont/SingularityLib/issues</url>
    </issueManagement>

    <properties>
        <java.version>25</java.version>
        <!-- Latest-only Paper 26.2+: resolves to the newest build on repo.papermc.io -->
        <paperapi.version>[26.2.build,)</paperapi.version>
        <!-- Last standalone Folia API line; from Paper 26.x Folia schedulers ship inside paper-api -->
        <foliaapi.version>26.2.build.7-beta</foliaapi.version>
        <!-- MockBukkit artifact matching Paper 26.2 -->
        <mockbukkit.version>4.116.1</mockbukkit.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>${java.version}</maven.compiler.release>
    </properties>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages (Pinont/SingularityLib)</name>
            <url>https://maven.pkg.github.com/pinont/SingularityLib</url>
        </repository>
    </distributionManagement>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>sonatype</id>
            <url>https://oss.sonatype.org/content/groups/public/</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <!-- Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <!-- Runs the singularitylib-processor annotation processor so the
                         lib's OWN @AutoRegister classes (e.g. EntityDamageListener) land
                         in a compile-time index inside the built jar. Consumers that
                         depend on the lib should add the same entry to generate their
                         own index (see README). -->
                    <annotationProcessorPaths>
                        <annotationProcessorPath>
                            <groupId>io.github.pinont</groupId>
                            <artifactId>singularitylib-processor</artifactId>
                            <version>${project.version}</version>
                        </annotationProcessorPath>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Resources -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>

            <!-- Sources JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Javadoc JAR (attached for Central) + HTML via javadoc:javadoc for the docs site.
                 failOnError stays false so missing comments / JDK 25 lint do not fail the build. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.7.0</version>
                <configuration>
                    <failOnError>false</failOnError>
                    <failOnWarnings>false</failOnWarnings>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>

            <!-- Explicit deploy plugin version -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>3.1.2</version>
            </plugin>
        </plugins>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paperapi.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- SQLite driver (bundled via -Dshade profile; consumers using SQLite keep it) -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.49.1.0</version>
        </dependency>
        <!-- SingularityNMS integration is DELIBERATELY reflection-only (NmsBridge):
             no Maven dependency declared, so the lib and its tests stay hermetically
             decoupled from net.minecraft.* server classes. Servers wanting deep-NBT
             install the SingularityNMS plugin; the bridge probes it at runtime. -->
        <!-- MongoDB sync driver -->
        <dependency>
            <groupId>org.mongodb</groupId>
            <artifactId>mongodb-driver-sync</artifactId>
            <version>5.10.0</version>
        </dependency>
        <dependency>
            <groupId>dev.folia</groupId>
            <artifactId>folia-api</artifactId>
            <version>${foliaapi.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.jetbrains</groupId>
            <artifactId>annotations</artifactId>
            <version>24.0.1</version>
        </dependency>
        <dependency>
            <groupId>org.reflections</groupId>
            <artifactId>reflections</artifactId>
            <version>0.10.2</version>
            <!-- Only the deprecated scanAndCollect fallback needs Reflections; consumers
                 compiled with the processor get a compile-time index instead. Optional so
                 it never leaks transitively into consumer builds. -->
            <optional>true</optional>
        </dependency>
        <!-- Compile-time @AutoRegister index generator. Provided: consumers do not need
             it on their runtime classpath (the index resource is baked into their jar
             at build time), and this also makes the lib's own build reference the
             module from the reactor. -->
        <dependency>
            <groupId>io.github.pinont</groupId>
            <artifactId>singularitylib-processor</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.dv8tion</groupId>
            <artifactId>JDA</artifactId>
            <version>5.0.0-beta.21</version>
        </dependency>
        <dependency>
            <groupId>org.mockbukkit.mockbukkit</groupId>
            <artifactId>mockbukkit-v26.2</artifactId>
            <version>${mockbukkit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>2.0.16</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>